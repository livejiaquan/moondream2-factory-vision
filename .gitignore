# ── 廠區機密資料（絕對不推）────────────────────────────────────────
usig/
HPC_download/
data/

# ── 執行輸出（本地保留，不推）────────────────────────────────────────
outputs/
evaluation/outputs/

# ── 本地工作區（不公開）─────────────────────────────────────────────
reports/
hpc_workspace/

# ── Python ──────────────────────────────────────────────────────────
__pycache__/
*.pyc
.venv/
*.egg-info/

# ── 模型快取 ────────────────────────────────────────────────────────
*.bin
*.safetensors

# ── 簡報 ────────────────────────────────────────────────────────────
moondream_pptx/

# ── 本地會話記憶 ─────────────────────────────────────────────────────
CLAUDE.md

# ── Demo 自測預覽圖（可由 --selftest 重新生成，不推）──────────────────
demo/_selftest_*.png

# ── 系統 ────────────────────────────────────────────────────────────
.DS_Store
.env

# ── 樣本圖片（公開授權，不推）────────────────────────────────────────
samples/
